__pycache__/
*.pyc
.env
# .mai 运行时数据整体忽略（会话记忆/trace/日志/向量库/快照/部署计划/个人笔记文档），
# 仅 Skill 体系（团队知识库）保留入库。
# 写法说明: 用 .mai/* 而不是 .mai/ —— 父目录被整体忽略时 git 无法 unignore 其子目录，
# 逐条忽略 .mai 下所有条目才能把 skills 重新放开；根级散落文件（SESSION_MEMORY.md 等）也被覆盖。
.mai/*
!.mai/skills/
!.mai/skills/**
# 独立 skill 项目不入库（ddo-code-flow 是独立项目，仅本地保留）
.mai/skills/ddo-code-flow/
dist/
build/
*.egg-info/
.pytest_cache/
_tmp_skill_repo/
node_modules/
desktop/dist/
desktop/out/
desktop/.vite/
*.spec
!backend.spec

# 个人文档（不提交）
/面试讲稿.md
# git 历史备份（本地保留，不入库）
*.bundle
