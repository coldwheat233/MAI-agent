# MAI-Agent 精品架构解析

> 一个以 Claude Code 架构为蓝本的 Python 个人开发助手平台，支持 CLI REPL、Web 桌面端、Electron 原生应用三种交互形态。

---

## 一、项目定位与核心痛点

### 1.1 现实痛点

在日常开发中，开发者面临几个核心效率瓶颈：

- **上下文断裂**：每轮对话丢失上一轮的思考成果，AI 像一个"金鱼脑"助手，反复问同样的问题
- **工具碎片化**：读代码用 IDE、搜代码用 grep、跑测试用终端——AI 助手如果只能聊天而不能操作文件系统，价值大打折扣
- **任务无结构**：用户说"帮我加个功能"，AI 直接动手写代码，没有先分析需求、拆解子任务、生成验收清单——质量完全靠运气
- **安全不可控**：让 AI 执行 Shell 命令是危险的——rm -rf、curl | sh 如果无审查直接执行，后果灾难性

### 1.2 设计目标

MAI-agent 的核心设计哲学是"**像人一样开发**"：先探索分析，再动手修改，最后验证闭合。整个项目围绕以下设计目标展开：

| 目标 | 含义 |
|------|------|
| **上下文持久化** | 跨会话记忆，不丢失关键决策和已知信息 |
| **多脑协作** | 不同阶段用不同"人格"的 AI 处理——探索者负责发散，验证者负责收敛 |
| **工具完备** | 文件读写、代码搜索、Shell 执行、Git 操作——一套工具打通开发全流程 |
| **安全护栏** | 三层门控（Hook 链 → 模式限制 → 沙箱审查），敏感操作可拦截可确认 |
| **扩展开放** | Plugin、MCP、Skill 三条扩展轨道，不锁定在单一生态 |

---

## 二、整体架构

### 2.1 分层架构图

```
┌─────────────────────────────────────────────────────┐
│                  用户交互层                          │
│   CLI (Click+Rich)  │  Web (FastAPI+WS)  │ Electron │
├─────────────────────────────────────────────────────┤
│                    会话引擎层                        │
│   AgentEngine — 生命周期管理 / 消息历史 / 统计       │
├─────────────────────────────────────────────────────┤
│                    核心循环层                        │
│   agent_loop — "思考→行动→观察" 无限循环             │
│   ┌─────────────────────────────────────────────┐   │
│   │  LLM 调用 → 解析 tool_calls → 执行工具     │   │
│   │    ↑                          ↓             │   │
│   │    └── 工具结果追加回 messages ──┘          │   │
│   └─────────────────────────────────────────────┘   │
├───────────────┬─────────────────┬───────────────────┤
│   工具系统    │   Hook 门控      │   四脑协调器       │
│   27个工具    │   执行前/后拦截  │   状态机调度        │
├───────────────┴─────────────────┴───────────────────┤
│                    基础设施层                        │
│   上下文注入  │  记忆系统  │  会话持久化  │  沙箱   │
│   三层模型    │  双阈值    │  JSON文件    │  审查   │
└─────────────────────────────────────────────────────┘
```

### 2.2 核心模块职责

**会话引擎** — 管家角色。负责创建 LLM 客户端、初始化工具注册表、管理消息历史。每轮对话提交到引擎后，引擎启动核心循环，循环结束后更新统计、触发后台记忆提取。引擎还负责协调器的生命周期——在 auto 模式下，每次提交都自动跑探索→验证循环，plan 模式只跑探索，manual 模式跳过。

**核心循环** — 心脏。这是完全自研的"思考→行动→观察"循环，不依赖任何 AI Agent 框架。循环内部做了上下文窗口管理——当 token 数超过阈值的 80% 时，自动将中间消息压缩为 LLM 生成的摘要，保留最近 8 条消息不压缩，确保不会丢失"最新记忆"。循环还支持流式输出、中断处理、Cancel 安全落盘。

**工具编排** — 双手。27 个工具统一通过注册表管理。每个工具声明自己的并发安全性（是否可并行执行）、名称、描述、参数 schema。主循环拿到 LLM 返回的 tool_calls 后，交给编排器分组执行——并发安全的工具并行跑，写操作的工具串行跑，最大化效率同时避免竞态。

**Hook 门控** — 免疫系统。在工具执行前，运行 Hook 链做三层判断：第一层跑所有注册的 PreToolUse Hook，任何一个返回 deny 就拦截；第二层是模式层——plan 模式只放行只读工具；第三层默认放行。Hook 还可以修改工具输入参数（比如自动校正文件路径），实现了"透明代理"模式。

---

## 三、核心链路：一次完整的用户请求

这是理解整个系统最关键的部分。以用户输入"帮我给这个项目加单元测试"为例：

### 第一段：上下文组装

```
用户输入
  ↓
引擎收到 submit() 调用
  ↓
重建 system prompt（三层注入）：
  Layer 1 — 系统信息：当前日期、工作目录、git status
  Layer 2 — 项目配置：CLAUDE.md 内容
  Layer 2.5 — 跨会话记忆：上轮对话的关键决策、文件变更
  Layer 2.55 — 标签化长期记忆：通过线段树索引按相关性检索
  Layer 2.6 — 可用 Skill 列表：让模型知道可以调用哪些 Skill
  Layer 3 — 脑上下文：如果激活了某个脑，注入角色指令
```

### 第二段：四脑协调（auto 模式）

```
协调器启动
  ↓
Triage 判断：用极短 LLM 调用（max_tokens=5，回答 YES/NO）
  判断这条消息是否需要结构化分析
  ↓ YES
探索阶段（dev_explorer 脑）：
  - 读取项目文件了解结构
  - 拆解需求为子任务
  - 生成验收清单写入 .mai/checklist_001.md
  ↓
验证阶段（dev_validator 脑）：
  - 逐项验证清单中的每个条目
  - 运行已有测试确认无回归
  - 闭合判断：全部通过 → COMPLETED / 有遗留 → BLOCKED
  ↓ BLOCKED → 将遗留项反馈给探索者重新规划
  ↓ COMPLETED → 分析结果注入 system prompt
```

### 第三段：核心循环执行

```
主循环启动
  ↓
Step 1: 调用 LLM（流式），携带 tools schema
  ↓
LLM 返回 tool_calls: [Read("src/main.py"), Grep(pattern="def test_")]
  ↓
权限门控：can_use_tool 跑 Hook 链 → 全部通过 → 放行
  ↓
工具编排：Read 和 Grep 都是并发安全的 → 并行执行
  ↓
工具结果追加回 messages
  ↓
Step 2: 再次调用 LLM（现在有了文件内容）
  ↓
LLM 返回 tool_calls: [Write("tests/test_main.py", content=...)]
  ↓
再次过门控 → 放行 → 执行
  ↓
Step 3: LLM 返回纯文本 → "测试已添加，包括以下用例..."
  ↓
收敛！循环结束
```

### 第四段：后处理

```
记忆提取（后台异步）：
  - 检查是否满足双阈值（token 增长量 + 工具调用次数）
  - 安全窗口检查：最后一轮没有未完成的工具调用
  - 用独立 LLM 调用提取关键决策 → 写入 SESSION_MEMORY.md

概念检测（后台异步）：
  - 从用户输入中提取技术概念
  - 中/高复杂度自动加入学习队列

会话持久化：
  - 完整消息历史存为 JSON
  - 下次会话可直接恢复上下文
```

---

## 四、架构设计中的精巧决策

### 4.1 双阈值记忆提取

记忆提取不是每次对话都做，那样太贵且无意义。系统用了两个阈值联合判断：

- **Token 增长阈值**：上下文增长了至少 500 token（可配置）
- **工具调用阈值**：至少执行了 3 次工具调用（可配置）
- **安全窗口**：最后一轮 assistant 消息不能有未完成的 tool_calls（避免中途提取不完整信息）

这三个条件同时满足才触发提取。这确保了提取的时机恰到好处——有足够多的"新鲜信息"，且对话处于一个自然停顿点。

### 4.2 线段树加标签记忆

普通的向量检索记忆有个问题：相关性不精准。MAI-agent 引入线段树索引——将记忆按时间片段组织，每个片段打上语义标签。查询时先通过标签快速定位相关片段，再精细检索。这是一个轻量但高效的本地检索方案。

### 4.3 上下文自动压缩

LLM 的上下文窗口有限（DeepSeek 是 128K）。当消息历史超过阈值的 80% 时，系统自动触发压缩：

1. 保留 system prompt（完整的角色指令）
2. 保留最近 8 条消息（最新上下文，不能丢）
3. 中间的消息段 → 发送给 LLM 做一句话摘要
4. 摘要作为一条 SystemMessage 替代被移除的消息

这样既不会丢失"之前发生了什么"的认知，又释放了大量 token 空间。

### 4.4 Cancel 安全落盘

如果用户在工具执行中途按 Ctrl+C 强行停止，循环可能处于"assistant 消息已发出但还没收到回复"的状态。系统对 `CancelledError` 做了专门处理——检测到取消时，将已流出的部分内容追加到消息历史，标记"(已被用户停止)"，确保状态可以恢复，不会丢失"答了一半"的内容。

### 4.5 工作区隔离

会话文件的存储不是扁平的。不同项目目录有各自的工作区（通过路径 slug 标识），`~/.mai/workspaces.json` 维护全局索引。这样用户在不同项目间切换时，各自的会话历史完全隔离，互不干扰。

---

## 五、安全体系：三道防线

### 第一道：Hook 链（可编程）

所有工具执行前都经过 Hook 链。Hook 可以注册到特定工具名（支持正则匹配），做输入审查、参数修改、甚至直接拒绝。比如可以注册一个 Hook 拦截所有 Write 操作，要求目标路径必须在项目目录内。

### 第二道：模式限制（策略层）

- **plan 模式**：硬编码只读工具白名单，Write/Edit/Bash 等直接拒绝
- **manual 模式**：每次工具执行弹给用户确认（y/n）
- **auto 模式**：全自动，但 Hook 链仍然生效

### 第三道：沙箱审查（命令级）

对于 Bash 工具，有三档沙箱：

- **off**：不审查（开发环境）
- **default**：拦截高危命令模式（`rm -rf /`、`curl | sh`、`dd of=/dev/`、fork 炸弹等）
- **strict**：仅白名单命令 + 禁网络 + 路径写越界检查

严格模式下，即使用户说"帮我删掉所有文件"，Bash 工具也会因为 `rm` 不在白名单而被拒绝。

---

## 六、扩展体系：三轨道

### Plugin 轨道

在 `.mai/plugins/` 下放置 `plugin.json` 清单 + Python 入口文件，动态 import 后注册工具。Plugin 可以是 tool/hook/skill/mcp 四种类型，支持热加载。

### MCP 轨道

兼容 MCP 协议，可以连接外部 MCP 服务器（如数据库工具、云服务工具）。MCP 配置在 `.mcp.json`，引擎启动时自动启动 MCP 服务器子进程并注册其工具。

### Skill 轨道

Skill 是最轻量的扩展方式——一个 Markdown 文件即一个 Skill。YAML frontmatter 声明名称和触发场景，正文是给模型的指令。启动时自动注入 system prompt 的"可用 Skills"列表，模型通过 Skill 工具按名激活。

---

## 七、开发中遇到的典型问题与解决方案

### 问题一：LLM 返回的 tool_calls 参数 JSON 不规范

**现象**：偶尔出现 JSON 解析失败，导致工具调用中断。

**解决**：所有工具参数解析都包了一层安全解析函数，解析失败返回空 dict 而不是崩掉，让 LLM 在下一轮有机会修正。

### 问题二：Agent 答了一半被用户停止，状态丢失

**现象**：用户按 Ctrl+C 后，assistant 的部分内容没有保存到消息历史，下次恢复会话时会"丢一段"。

**解决**：核心循环整体包在一个 try-except CancelledError 里，检测到取消时检查 step_committed 标志位——如果当前 step 的 assistant 消息还没追加，就补一条包含已流出文本的消息，确保"半成品"也被保存。

### 问题三：中文 Windows 下编码问题

**现象**：PowerShell/cmd 的 stdout 默认 GBK 编码，Rich 渲染的 Unicode 字符（如 ✅❌）会乱码。

**解决**：CLI 入口最优先执行 `sys.stdout.reconfigure(encoding="utf-8")`，server 模式也做同样处理，同时在 subprocess 调用时显式设置环境变量。

### 问题四：跨轮次对话时脑上下文残留

**现象**：上一轮激活了 dev_explorer 脑，下一轮切换话题后系统 prompt 里还带着探索者的角色指令，导致 LLM 行为异常。

**解决**：在每轮核心循环开始时，先清除所有以特定前缀开头的 system message，再根据当前 active_brain 重新注入正确的脑上下文。

### 问题五：会话文件定位在 worktree 场景下失败

**现象**：用户用 git worktree 切换到分支副本后，cwd 变了，之前保存的 session 找不到了。

**解决**：引入工作区系统——每个项目目录生成唯一 slug，session 文件存在工作区目录下而非项目根目录。同时维护全局索引，支持跨工作区按 session ID 全局查找。

### 问题六：多轮对话中 git status 信息过时

**现象**：system prompt 中的 git status 是启动时缓存的，对话中文件变更后不会刷新，LLM 看到的是过时的 git 状态。

**解决**：系统上下文重建函数在每次 submit 前通过线程池调用（避免阻塞事件循环），同时清除缓存，确保每次都拿到最新的 git status。

---

## 八、总结

MAI-agent 并不是简单地"给 LLM 套一个工具调用的壳"——它解决的是**让 AI 像人类开发者一样工作**这个核心命题。通过"探索→执行→验证"的闭环、三道防线的安全体系、双阈值记忆系统、上下文自动压缩等设计，构建了一个完整的开发助手架构。

它的核心价值在于**打通了从需求到代码到验证的完整链路**，并且这个链路是闭环的——验证失败会回退到探索阶段重新规划，而不是一条路走到黑。

未来方向包括：更丰富的部署规划脑、向量化长期记忆、多模态输入支持、以及更完善的团队协作工作区。这些都是建立在当前稳固架构基础上的自然延伸。
